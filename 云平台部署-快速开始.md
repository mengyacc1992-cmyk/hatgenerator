# ⚡ 云平台部署 - 5分钟快速开始

## 🎯 目标

把前端部署到 Vercel，后端部署到 Render，让全世界都能访问你的应用！

---

## 📋 准备工作（1分钟）

### 1. 准备 API Key
- 去 https://aistudio.google.com/apikey 申请
- 复制你的 API Key，后面要用

### 2. 准备 GitHub 账号
- 如果没有，去 https://github.com 注册
- 确保代码已推送到 GitHub

---

## 🚀 第一步：部署后端到 Render（3分钟）

### 1. 打开 Render
- 访问 https://render.com
- 用 GitHub 登录

### 2. 创建服务
1. 点击 **"New +"** → **"Web Service"**
2. 连接你的 `hatgenerator` 仓库
3. 配置如下：

**关键配置：**
- **Name**: `hatgenerator-backend`
- **Root Directory**: `server` ⚠️ 重要！
- **Build Command**: `npm install && npm run build`
- **Start Command**: `npm start`

**环境变量（点击 "Advanced" → "Add Environment Variable"）：**
```
GEMINI_API_KEY = 你的API_Key
NODE_ENV = production
CORS_ORIGIN = https://你的vercel地址.vercel.app（先填临时值）
```

### 3. 部署
- 点击 **"Create Web Service"**
- 等待 5-10 分钟
- **记下服务地址**，比如：`https://hatgenerator-backend.onrender.com`

### 4. 测试
访问：`https://你的地址.onrender.com/api/health`
应该看到：`{"status":"ok"}`

---

## 🌐 第二步：部署前端到 Vercel（2分钟）

### 1. 打开 Vercel
- 访问 https://vercel.com
- 用 GitHub 登录

### 2. 创建项目
1. 点击 **"Add New..."** → **"Project"**
2. 导入你的 `hatgenerator` 仓库

### 3. 配置项目
**框架设置（自动检测）：**
- Framework: `Vite`
- Build Command: `npm run build`
- Output Directory: `dist`

**环境变量（点击 "Environment Variables"）：**
```
VITE_API_BASE_URL = https://你的render地址.onrender.com
```

### 4. 部署
- 点击 **"Deploy"**
- 等待 2-3 分钟
- 看到成功提示和网址

### 5. 更新 Render 的 CORS
1. 回到 Render，找到你的服务
2. 点击 **"Environment"**
3. 更新 `CORS_ORIGIN` 为你的 Vercel 地址
4. 保存（会自动重新部署）

---

## ✅ 完成！

现在访问你的 Vercel 网址，应该可以正常使用了！

---

## 🆘 遇到问题？

### 前端无法连接后端
- 检查 `VITE_API_BASE_URL` 是否正确
- 检查 Render 服务是否运行（状态为 Live）

### 后端 API 调用失败
- 检查 `GEMINI_API_KEY` 是否正确
- 检查 Render 日志中的错误

### 部署失败
- 查看构建日志
- 确认配置是否正确

**详细说明**：查看 `Vercel部署指南-小白版.md` 和 `Render部署指南-小白版.md`

---

## 📚 详细文档

- **完整流程**：`云平台部署-完整流程.md`
- **Vercel 详细**：`Vercel部署指南-小白版.md`
- **Render 详细**：`Render部署指南-小白版.md`
