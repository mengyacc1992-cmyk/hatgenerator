# 🚀 启动命令（快速参考）

## 前端服务

**目录：** `/Users/chengchi/yadamengisgenius-1/hatgenerator`

**命令：**
```bash
cd /Users/chengchi/yadamengisgenius-1/hatgenerator
npm run dev
```

## 后端服务

**目录：** `/Users/chengchi/yadamengisgenius-1/hatgenerator/server`

**命令：**
```bash
cd /Users/chengchi/yadamengisgenius-1/hatgenerator/server
npm run dev
```

## 一键启动（两个服务）

**目录：** `/Users/chengchi/yadamengisgenius-1/hatgenerator`

**命令：**
```bash
cd /Users/chengchi/yadamengisgenius-1/hatgenerator
./start.sh
```

## 如何确认在正确目录？

运行 `pwd` 命令查看当前目录：

**前端：**
```bash
pwd
# 应该显示：/Users/chengchi/yadamengisgenius-1/hatgenerator
```

**后端：**
```bash
pwd
# 应该显示：/Users/chengchi/yadamengisgenius-1/hatgenerator/server
```

## 常见错误

### 错误：找不到 package.json

**原因：** 在错误的目录下运行了命令

**解决方法：** 先进入正确的目录，再运行命令

### 错误：端口已被占用

**解决方法：**
```bash
# 停止占用端口的进程
lsof -ti:3000 | xargs kill -9  # 前端端口
lsof -ti:3001 | xargs kill -9  # 后端端口
```
