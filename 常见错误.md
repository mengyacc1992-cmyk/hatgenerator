# 🐛 常见错误解决指南

## 错误：找不到 package.json

### 错误信息
```
npm error enoent Could not read package.json
npm error path /Users/chengchi/package.json
```

### 原因
你在错误的目录下运行了 `npm` 命令。

### 解决方法

**步骤 1：进入项目目录**
```bash
cd /Users/chengchi/yadamengisgenius-1/hatgenerator
```

**步骤 2：确认在正确目录**
```bash
# 查看当前目录
pwd

# 应该显示：
# /Users/chengchi/yadamengisgenius-1/hatgenerator

# 查看文件
ls

# 应该能看到 package.json 文件
```

**步骤 3：运行命令**
```bash
npm run dev
```

### 一行命令（推荐）
```bash
cd /Users/chengchi/yadamengisgenius-1/hatgenerator && npm run dev
```

## 如何确认在正确目录？

### 方法 1：查看终端提示符
- 正确：`chengchi@ChengdeMacBook-Air hatgenerator %`
- 错误：`chengchi@ChengdeMacBook-Air ~ %`（~ 表示在家目录）

### 方法 2：运行 pwd 命令
```bash
pwd
```
应该显示：`/Users/chengchi/yadamengisgenius-1/hatgenerator`

### 方法 3：查看文件
```bash
ls package.json
```
如果显示 `package.json`，说明在正确目录。

## 其他常见错误

### 错误：端口已被占用
```
Error: listen EADDRINUSE: address already in use :::3000
```

**解决方法：**
```bash
# 找到占用端口的进程并停止
lsof -ti:3000 | xargs kill -9
```

### 错误：找不到模块
```
Error: Cannot find module 'xxx'
```

**解决方法：**
```bash
# 重新安装依赖
npm install
```

### 错误：权限被拒绝
```
Error: EACCES: permission denied
```

**解决方法：**
```bash
# 使用 sudo（谨慎使用）
sudo npm install
```

## 快速检查清单

运行命令前，确认：
- ✅ 在正确的项目目录
- ✅ 已安装依赖（有 node_modules 文件夹）
- ✅ 端口没有被占用
- ✅ 网络连接正常
