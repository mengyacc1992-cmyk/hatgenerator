# ⚡ 快速部署指南

## 🎯 三种部署方式

### 方式一：开发环境（最简单）

```bash
# 1. 启动后端
cd server
npm run dev

# 2. 启动前端（新终端）
cd ..
npm run dev
```

✅ **适合**：本地开发和测试

---

### 方式二：PM2 部署（推荐）

```bash
# 1. 安装 PM2
npm install -g pm2

# 2. 构建后端
cd server
npm install
npm run build

# 3. 启动服务
pm2 start ecosystem.config.js

# 4. 查看状态
pm2 status
pm2 logs hatgenerator-backend
```

✅ **适合**：服务器部署，需要进程管理

📖 **详细说明**：查看 `PM2部署指南.md`

---

### 方式三：Docker 部署

```bash
# 1. 配置环境变量
# 编辑 server/.env 文件

# 2. 启动服务
docker-compose up -d

# 3. 查看日志
docker-compose logs -f
```

✅ **适合**：生产环境，需要容器化部署

📖 **详细说明**：查看 `部署指南.md`

---

## 📋 部署前检查清单

- [ ] API Key 已配置（`server/.env` 中的 `GEMINI_API_KEY`）
- [ ] 依赖已安装（`npm install` 在根目录和 `server` 目录）
- [ ] 端口未被占用（3000 和 3001）
- [ ] 网络连接正常（VPN 如需要）

---

## 🚀 一键部署脚本

```bash
# 使用部署脚本
./deploy.sh
```

---

## 📚 更多信息

- **完整部署指南**：`部署指南.md`
- **PM2 使用**：`PM2部署指南.md`
- **架构说明**：`server/ARCHITECTURE.md`
